<img src="~/Images/smoke.jpg" class="img-fluid" style="position:absolute;top:0px;left:0px;z-index:-1;width:100%;" />
<div class="bg-light p-4 rounded-left" style="opacity:.8;width:50%;">
    <h2>Here are your three wishes</h2>
    <ul>
        <!--access data and temp data are storing things in the value pairs (keys/values)
            temp data dictionary is different then viewData. -->
        @foreach (var wish in TempData["WishList"] as IList<string>)  
        {
            <li>@wish</li>
        }
    </ul>
</div>
